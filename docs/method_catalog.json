[
  {
    "name": "binomial_test",
    "family": "Calibration",
    "returns": "pandas.DataFrame\n    One row per observed grade: Rating class, Predicted PD (mean), Total count, Defaults, Actual Default Rate, p_value, Reject H0 (boolean).",
    "notes": "p_value = P[Binomial(N, mean predicted PD) >= D]. The alternative is underestimated default probability; reject when p_value < alpha_level. Requires independent obligors with a common PD per grade. Averaging heterogeneous PDs is an approximation, not a Poisson-binomial test. Grade-wise decisions are unadjusted for multiplicity.",
    "raises": "Invalid columns, binary outcomes, probabilities, or significance level.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'grade': ['A'] * 4 + ['B'] * 4, 'outcome': [0, 0, 1, 1] * 2, 'pd': [.2] * 4 + [.6] * 4})\nresult = m.binomial_test(data, 'grade', 'outcome', 'pd')\nassert np.allclose(result.p_value, [.1808, .8208])\nassert result['Reject H0'].tolist() == [False, False]",
    "check": "assert np.allclose(result.p_value, [.1808, .8208])\nassert result['Reject H0'].tolist() == [False, False]",
    "interpretation": "Neither grade rejects at 5%. Four observations per grade give little power; non-rejection does not establish calibration.",
    "reference": "https://www.bis.org/publ/bcbs_wp14.pdf",
    "test": "tests/test_methods.py::test_binomial_test",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "ratings": [
        "str",
        "Name of the non-missing rating-grade column. Calibration grouping uses observed grades only."
      ],
      "default_flag": [
        "str",
        "Binary 0/1 outcome column name; booleans are accepted."
      ],
      "predicted_pd": [
        "str",
        "Finite predicted probability column name, values in [0, 1]."
      ],
      "alpha_level": [
        "float, default 0.05",
        "Tail threshold strictly between 0 and 1. Reject for a strictly smaller documented tail probability."
      ]
    },
    "summary": "Test for underestimated default probability in each grade.",
    "title": "Binomial test"
  },
  {
    "name": "brier_score",
    "family": "Calibration",
    "returns": "float\n    Mean squared error in [0, 1]; lower is better.",
    "notes": "Brier = mean((outcome - predicted PD)**2) over individual observations. Grades are validated but do not alter weighting; ratings is retained for compatibility. This proper scoring rule measures calibration and discrimination, depends on prevalence, and has no null hypothesis or p-value.",
    "raises": "Invalid columns, nonbinary outcomes, or PDs outside [0, 1].",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'grade': ['A'] * 4 + ['B'] * 4, 'outcome': [0, 0, 1, 1] * 2, 'pd': [.2] * 4 + [.6] * 4})\nresult = m.brier_score(data, 'grade', 'outcome', 'pd')\nassert np.isclose(result, .30)",
    "check": "assert np.isclose(result, .30)",
    "interpretation": "Mean squared probability error is 0.30. Compare models on the same observations and event definition.",
    "reference": "https://scikit-learn.org/stable/modules/generated/sklearn.metrics.brier_score_loss.html",
    "test": "tests/test_methods.py::test_brier_score",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "ratings": [
        "str",
        "Grade column name, validated for compatibility. The statistic uses individual observations without grade aggregation."
      ],
      "default_flag": [
        "str",
        "Binary 0/1 outcome column name; booleans are accepted."
      ],
      "predicted_pd": [
        "str",
        "Finite predicted probability column name, values in [0, 1]."
      ]
    },
    "summary": "Calculate the observation-level mean squared probability error.",
    "title": "Brier score"
  },
  {
    "name": "herfindahl_test",
    "family": "Stability",
    "returns": "tuple of float\n    (coefficient_of_variation, classic_Herfindahl_index).",
    "notes": "For K grades and shares s, CV=sqrt(K*sum((s-1/K)**2)); HHI=sum(s**2), ranging from 1/K to 1. Larger means more concentration. This classic HHI differs from the logarithmic ECB transformation. No p-value or alpha parameter applies. Empty grades in rating_order affect K and CV.",
    "raises": "Missing grades or invalid/incomplete rating order.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'grade': ['A'] * 4 + ['B'] * 2})\nresult = m.herfindahl_test(data, 'grade')\nassert np.allclose(result, [1 / 3, 5 / 9])",
    "check": "assert np.allclose(result, [1 / 3, 5 / 9])",
    "interpretation": "HHI=5/9 exceeds the two-grade uniform baseline of 1/2. Compare on the same grade universe.",
    "reference": "https://www.bankingsupervision.europa.eu/activities/internal_models/shared/pdf/instructions_validation_reporting_credit_risk.en.pdf",
    "test": "tests/test_methods.py::test_herfindahl_test",
    "parameters": {
      "data1": [
        "pandas.DataFrame",
        "Nonempty initial/single-period portfolio with a non-missing grade column; not modified."
      ],
      "ratings": [
        "str",
        "Name of the non-missing rating-grade column. Calibration grouping uses observed grades only."
      ],
      "rating_order": [
        "sequence, optional",
        "Unique complete grade labels from lowest to highest; unobserved grades are retained. Otherwise use consistent ordered categorical metadata, then naturally sort the observed union. Specify business order explicitly."
      ]
    },
    "summary": "Measure concentration of one portfolio across rating grades.",
    "title": "Herfindahl index"
  },
  {
    "name": "herfindahl_multiple_period_test",
    "family": "Stability",
    "returns": "pandas.DataFrame\n    Grade-indexed N_initial and N_current. Summary row total also contains h_initial, h_current, z_stat, p_value and nullable-boolean reject; other statistic cells are missing.",
    "notes": "Use the union of grades, retaining zero counts, or rating_order. For initial/current CVs c1/c2, z=sqrt(K-1)*(c2-c1)/sqrt(c2**2*(0.5+c2**2)); p_value=normal.sf(z). Reject increased concentration when p_value < alpha_level. This is the ECB asymptotic CV comparison; HHI columns use classic squared shares. It is undefined for zero current CV and is not a paired-account test.",
    "raises": "Invalid data/order/alpha, fewer than two grades, uniform current shares, or reserved grade label 'total'.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ninitial = pd.DataFrame({'grade': ['A'] * 4 + ['B'] * 2})\ncurrent = pd.DataFrame({'grade': ['A'] * 5 + ['B']})\nresult = m.herfindahl_multiple_period_test(initial, current, 'grade')\nassert np.isclose(result.loc['total', 'z_stat'], 3 / np.sqrt(34))\nassert np.isclose(result.loc['total', 'h_current'], 26 / 36)",
    "check": "assert np.isclose(result.loc['total', 'z_stat'], 3 / np.sqrt(34))\nassert np.isclose(result.loc['total', 'h_current'], 26 / 36)",
    "interpretation": "Concentration increased, but this statistic does not reject at 5%. Six accounts illustrate arithmetic only.",
    "reference": "https://www.bankingsupervision.europa.eu/activities/internal_models/shared/pdf/instructions_validation_reporting_credit_risk.en.pdf",
    "test": "tests/test_methods.py::test_herfindahl_multiple_period_test",
    "parameters": {
      "data1": [
        "pandas.DataFrame",
        "Nonempty initial/single-period portfolio with a non-missing grade column; not modified."
      ],
      "data2": [
        "pandas.DataFrame",
        "Nonempty current portfolio. Its size may differ from data1; not modified."
      ],
      "ratings": [
        "str",
        "Name of the non-missing rating-grade column. Calibration grouping uses observed grades only."
      ],
      "alpha_level": [
        "float, default 0.05",
        "Tail threshold strictly between 0 and 1. Reject for a strictly smaller documented tail probability."
      ],
      "rating_order": [
        "sequence, optional",
        "Unique complete grade labels from lowest to highest; unobserved grades are retained. Otherwise use consistent ordered categorical metadata, then naturally sort the observed union. Specify business order explicitly."
      ]
    },
    "summary": "Test whether grade concentration increased using the ECB CV statistic.",
    "title": "Multiple-period Herfindahl test"
  },
  {
    "name": "hosmer_test",
    "family": "Calibration",
    "returns": "list\n    [p_value, reject], with boolean rejection.",
    "notes": "Q=sum((D-N*p)**2/(N*p*(1-p))); p_value=chi2.sf(Q,K-ddof). Default ddof=0 tests fixed externally specified grade PDs with K degrees of freedom. Explicit ddof=2 selects the conventional fitted-logistic Hosmer-Lemeshow approximation with appropriate grouping. Requires independent outcomes, homogeneous grade PDs and sufficiently large expected default/nondefault counts. Reject any departure when p_value < alpha_level.",
    "raises": "Invalid data/alpha, boundary grade mean PDs, or noninteger ddof outside 0 <= ddof < K.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'grade': ['A'] * 4 + ['B'] * 4, 'outcome': [0, 0, 1, 1] * 2, 'pd': [.2] * 4 + [.6] * 4})\nresult = m.hosmer_test(data, 'grade', 'outcome', 'pd')\nassert np.isclose(result[0], np.exp(-(2.25 + 1 / 6) / 2))\nassert result[1] is False",
    "check": "assert np.isclose(result[0], np.exp(-(2.25 + 1 / 6) / 2))\nassert result[1] is False",
    "interpretation": "The fixed-PD test does not reject. Tiny counts make the chi-square inference unreliable.",
    "reference": "https://www.bis.org/publ/bcbs_wp14.pdf",
    "test": "tests/test_methods.py::test_hosmer_test",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "ratings": [
        "str",
        "Name of the non-missing rating-grade column. Calibration grouping uses observed grades only."
      ],
      "default_flag": [
        "str",
        "Binary 0/1 outcome column name; booleans are accepted."
      ],
      "predicted_pd": [
        "str",
        "Finite predicted probability column name, values in [0, 1]."
      ],
      "alpha_level": [
        "float, default 0.05",
        "Tail threshold strictly between 0 and 1. Reject for a strictly smaller documented tail probability."
      ],
      "ddof": [
        "int, default 0",
        "Degrees of freedom deducted from observed grade count K; 0 <= ddof < K. Zero tests fixed external PDs; 2 selects fitted-logistic Hosmer-Lemeshow inference."
      ]
    },
    "summary": "Apply a grouped chi-square calibration test to grade-level default counts.",
    "title": "Hosmer calibration test"
  },
  {
    "name": "spiegelhalter_test",
    "family": "Calibration",
    "returns": "tuple\n    (z_statistic, reject), with two-sided boolean rejection.",
    "notes": "z=sum((y-p)*(1-2*p))/sqrt(sum(p*(1-p)*(1-2*p)**2)), using individual observations. Under independent Bernoulli outcomes with correct fixed PDs the statistic is approximately normal. Reject when 2*normal.sf(abs(z)) < alpha_level. Calibration errors can cancel. PDs of 0, 0.5 or 1 contribute zero null variance; entirely zero variance makes inference undefined.",
    "raises": "Invalid data/alpha or zero null variance.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'grade': ['A'] * 4 + ['B'] * 4, 'outcome': [0, 0, 1, 1] * 2, 'pd': [.2] * 4 + [.6] * 4})\nresult = m.spiegelhalter_test(data, 'grade', 'outcome', 'pd')\nassert np.isclose(result[0], .8 / np.sqrt(.2688))\nassert result[1] is False",
    "check": "assert np.isclose(result[0], .8 / np.sqrt(.2688))\nassert result[1] is False",
    "interpretation": "The statistic is about 1.54 and does not reject at 5% under the two-sided convention.",
    "reference": "https://doi.org/10.1002/sim.4780050506",
    "test": "tests/test_methods.py::test_spiegelhalter_test",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "ratings": [
        "str",
        "Grade column name, validated for compatibility. The statistic uses individual observations without grade aggregation."
      ],
      "default_flag": [
        "str",
        "Binary 0/1 outcome column name; booleans are accepted."
      ],
      "predicted_pd": [
        "str",
        "Finite predicted probability column name, values in [0, 1]."
      ],
      "alpha_level": [
        "float, default 0.05",
        "Tail threshold strictly between 0 and 1. Reject for a strictly smaller documented tail probability."
      ]
    },
    "summary": "Test probability calibration with the observation-level Spiegelhalter z statistic.",
    "title": "Spiegelhalter test"
  },
  {
    "name": "jeffreys_test",
    "family": "Calibration",
    "returns": "pandas.DataFrame\n    Grade summary columns as in binomial_test, plus posterior lower-tail p_value and boolean Reject H0.",
    "notes": "A Beta(1/2,1/2) prior and D defaults in N independent trials yield Beta(D+1/2,N-D+1/2). Return its CDF at mean predicted PD. A small tail indicates predicted PD lies below most posterior mass; Reject H0 uses p_value < alpha_level. This posterior tail is not an exact frequentist p-value. Common grade PDs and independence are assumed; no multiplicity adjustment is applied.",
    "raises": "Invalid columns, outcomes, probabilities, or significance level.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'grade': ['A'] * 4, 'outcome': [0, 0, 1, 1], 'pd': [.5] * 4})\nresult = m.jeffreys_test(data, 'grade', 'outcome', 'pd')\nassert np.isclose(result.p_value.iloc[0], .5)",
    "check": "assert np.isclose(result.p_value.iloc[0], .5)",
    "interpretation": "The symmetric posterior has lower-tail probability 0.5 at the forecast PD.",
    "reference": "https://www.bis.org/publ/bcbs_wp14.pdf",
    "test": "tests/test_methods.py::test_jeffreys_test",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "ratings": [
        "str",
        "Name of the non-missing rating-grade column. Calibration grouping uses observed grades only."
      ],
      "default_flag": [
        "str",
        "Binary 0/1 outcome column name; booleans are accepted."
      ],
      "predicted_pd": [
        "str",
        "Finite predicted probability column name, values in [0, 1]."
      ],
      "alpha_level": [
        "float, default 0.05",
        "Tail threshold strictly between 0 and 1. Reject for a strictly smaller documented tail probability."
      ]
    },
    "summary": "Calculate a Jeffreys-posterior lower-tail probability for each rating grade.",
    "title": "Jeffreys test"
  },
  {
    "name": "roc_auc",
    "family": "Discrimination",
    "returns": "float\n    AUC in [0, 1]; 0.5 means random pairwise ordering.",
    "notes": "AUC=P(score of outcome 1 > score of outcome 0)+0.5*P(tie). Larger finite scores must mean greater default risk; scores need not be probabilities. Both binary classes are required. Uses scikit-learn ROC AUC. This descriptive measure has no p-value and does not measure calibration.",
    "raises": "Invalid columns, nonfinite scores, nonbinary target or an absent class.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'y': [0, 0, 1, 1], 'score': [1, 2, 2, 3]})\nresult = m.roc_auc(data, 'y', 'score')\nassert np.isclose(result, .875)",
    "check": "assert np.isclose(result, .875)",
    "interpretation": "Three positive-negative pairs are correctly ordered and one ties: (3+0.5)/4=0.875.",
    "reference": "https://scikit-learn.org/stable/modules/generated/sklearn.metrics.roc_auc_score.html",
    "test": "tests/test_methods.py::test_roc_auc",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "target": [
        "str",
        "Binary target column name, with both 0 and 1 present."
      ],
      "prediction": [
        "str",
        "Finite numeric score column name. Larger means outcome 1; scores need not be probabilities."
      ]
    },
    "summary": "Calculate binary ROC area, assigning half credit to tied scores.",
    "title": "ROC AUC"
  },
  {
    "name": "gini",
    "family": "Discrimination",
    "returns": "float\n    Normalized Gini in [-1, 1].",
    "notes": "Gini=2*ROC_AUC-1, with higher scores for outcome 1 and half credit for ties. Values 1, 0 and -1 indicate perfect, random and reverse ordering. Requires both classes and finite scores. This is the discrimination Gini, not an income-inequality estimator or a calibration significance test.",
    "raises": "Invalid classification data; see roc_auc.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'y': [0, 0, 1, 1], 'score': [1, 2, 2, 3]})\nresult = m.gini(data, 'y', 'score')\nassert np.isclose(result, .75)",
    "check": "assert np.isclose(result, .75)",
    "interpretation": "An AUC of 0.875 corresponds to Gini 0.75.",
    "reference": "https://scikit-learn.org/stable/modules/generated/sklearn.metrics.roc_auc_score.html",
    "test": "tests/test_methods.py::test_gini",
    "parameters": {
      "df": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "target": [
        "str",
        "Binary target column name, with both 0 and 1 present."
      ],
      "prediction": [
        "str",
        "Finite numeric score column name. Larger means outcome 1; scores need not be probabilities."
      ]
    },
    "summary": "Calculate the normalized discrimination Gini as twice ROC AUC minus one.",
    "title": "Gini coefficient"
  },
  {
    "name": "kolmogorov_smirnov_stat",
    "family": "Discrimination",
    "returns": "object\n    SciPy KstestResult with statistic, pvalue and location/sign metadata.",
    "notes": "D is the largest absolute difference between score CDFs conditional on outcome 0 and outcome 1. Null: equal score distributions; alternative: two-sided. SciPy ks_2samp selects exact/asymptotic inference automatically. It assumes independent samples; continuous-distribution p-values are approximate for tied/discrete scores. D measures separation irrespective of score direction, not PD calibration.",
    "raises": "Invalid classification columns, scores or classes.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'y': [0, 0, 1, 1], 'score': [1, 2, 3, 4]})\nresult = m.kolmogorov_smirnov_stat(data, 'y', 'score')\nassert np.isclose(result.statistic, 1)\nassert np.isclose(result.pvalue, 1 / 3)",
    "check": "assert np.isclose(result.statistic, 1)\nassert np.isclose(result.pvalue, 1 / 3)",
    "interpretation": "Class score ranges do not overlap (D=1), but the exact two-sided p-value is 1/3 for two observations per class.",
    "reference": "https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.ks_2samp.html",
    "test": "tests/test_methods.py::test_kolmogorov_smirnov_stat",
    "parameters": {
      "df": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "target": [
        "str",
        "Binary target column name, with both 0 and 1 present."
      ],
      "prediction": [
        "str",
        "Finite numeric score column name. Larger means outcome 1; scores need not be probabilities."
      ]
    },
    "summary": "Compare score distributions of the two outcome classes using two-sample KS.",
    "title": "Kolmogorov–Smirnov statistic"
  },
  {
    "name": "cumulative_lgd_accuracy_ratio",
    "family": "Discrimination",
    "returns": "float\n    Twice the cumulative ordinal ROC area, in [0, 1].",
    "notes": "At each threshold from highest to lowest grade, x=P(predicted >= threshold), y=P(predicted >= threshold AND realised >= threshold). Include (0,0), integrate whole tied-grade bands by trapezoids and return twice the area. This follows VUROCS clar (Ozdemir and Miu convention). It is ordinal accuracy, not a chance-adjusted Gini. Predictions and outcomes share a grade scale, higher meaning more loss. Accounts have equal weight; no p-value applies.",
    "raises": "Invalid columns, missing grades or invalid/ambiguous grade order.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'p': [1, 2, 3, 3, 4], 'y': [1, 3, 2, 4, 4]})\nresult = m.cumulative_lgd_accuracy_ratio(data, 'p', 'y')\nassert np.isclose(result, .88)",
    "check": "assert np.isclose(result, .88)",
    "interpretation": "Threshold points (0,0), (.2,.2), (.6,.4), (.8,.8), (1,1) give twice the trapezoidal area 0.88.",
    "reference": "https://cran.r-universe.dev/VUROCS/VUROCS.pdf",
    "test": "tests/test_methods.py::test_cumulative_lgd_accuracy_ratio",
    "parameters": {
      "df": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "predicted_ratings": [
        "str",
        "Predicted ordinal loss-grade column name; higher grades mean greater loss."
      ],
      "realised_outcomes": [
        "str",
        "Realised ordinal loss-grade column name, on the same scale as predictions."
      ],
      "rating_order": [
        "sequence, optional",
        "Unique complete grade labels from lowest to highest; unobserved grades are retained. Otherwise use consistent ordered categorical metadata, then naturally sort the observed union. Specify business order explicitly."
      ]
    },
    "summary": "Calculate the VUROCS cumulative LGD accuracy measure for ordinal grades.",
    "title": "Cumulative LGD accuracy ratio"
  },
  {
    "name": "loss_capture_ratio",
    "family": "Discrimination",
    "returns": "float\n    Model/ideal area gain ratio in [-1, 1], up to floating-point error.",
    "notes": "Sort predicted LGD descending; x=cumulative EAD share, y=cumulative realised monetary loss share. Pool tied scores and include the origin. The ideal curve sorts realised LGD; LCR=(area_model-0.5)/(area_ideal-0.5). This explicitly uses exposure on the x axis; account-count variants differ. Perfect/reverse ordering yields 1/-1; constant predicted scores yield 0. Zero EAD has no influence. This is descriptive with no p-value.",
    "raises": "Invalid paired LGDs/exposures, nonpositive total EAD/loss, or zero ideal gain (constant LGD on positive exposures).",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\nresult = m.loss_capture_ratio([1, 1, 1], [.1, .4, .9], [.1, .4, .9])\nassert np.isclose(result, 1)",
    "check": "assert np.isclose(result, 1)",
    "interpretation": "Sorting on true loss rates reproduces the ideal curve. Match the area convention when comparing implementations.",
    "reference": "https://aptivaa.com/pdf/ifrs9-model-risk-management-1594098423-1.pdf",
    "test": "tests/test_methods.py::test_loss_capture_ratio",
    "parameters": {
      "ead": [
        "array-like",
        "Finite nonnegative exposures with positive total, positionally paired with LGDs."
      ],
      "predicted_ratings": [
        "array-like",
        "Finite predicted LGD fractions in [0, 1], despite the historical ratings name."
      ],
      "realised_outcomes": [
        "array-like",
        "Equal-length finite realised LGD fractions in [0, 1]. Series indices are ignored."
      ]
    },
    "summary": "Compare model and ideal loss-capture gains using cumulative exposure on the x axis.",
    "title": "Loss capture ratio"
  },
  {
    "name": "bayesian_error_rate",
    "family": "Discrimination",
    "returns": "float\n    Minimum empirical misclassification fraction in [0, 0.5], unrounded.",
    "notes": "For every threshold, error=(1-prevalence)*FPR+prevalence*(1-TPR). Return the minimum, including all/none-positive predictions. This is empirical threshold-optimized error with equal costs, not irreducible Bayes error. Higher scores mean outcome 1. Training-set optimization is optimistic: evaluate on held-out data. Requires both classes; returns no p-value.",
    "raises": "Invalid classification data or an absent outcome class.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'y': [0, 0, 1, 1], 'score': [1, 2, 2, 3]})\nresult = m.bayesian_error_rate(data, 'y', 'score')\nassert np.isclose(result, .25)",
    "check": "assert np.isclose(result, .25)",
    "interpretation": "A positive and negative tie at score 2, so at least one of four observations is misclassified.",
    "reference": "https://scikit-learn.org/stable/modules/generated/sklearn.metrics.roc_curve.html",
    "test": "tests/test_methods.py::test_bayesian_error_rate",
    "parameters": {
      "df": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "default_flag": [
        "str",
        "Binary 0/1 outcome column name; booleans are accepted."
      ],
      "prob_default": [
        "str",
        "Finite numeric score column name. Larger means outcome 1; unbounded scores are accepted despite the historical name."
      ]
    },
    "summary": "Find the minimum empirical misclassification rate over score thresholds.",
    "title": "Bayesian error rate"
  },
  {
    "name": "information_value",
    "family": "Discrimination",
    "returns": "tuple\n    (table, IV). Bin-indexed table columns: good, bad (raw counts), good_share, bad_share (smoothed), WoE, IV; the scalar sums contributions.",
    "notes": "Pre-bin the feature. Add smoothing to every bin/class count and normalize classes separately. WoE=log(good_share/bad_share); IV=sum((good_share-bad_share)*WoE). Outcome 0 is good, 1 bad. Default smoothing=0.5 makes empty cells finite; smoothing=0 requires positive cells. Uses the union of observed bins. Descriptive and nonnegative, with no universal acceptance threshold or p-value. Sparse or selected bins can inflate IV.",
    "raises": "Invalid columns/classes/smoothing, or zero cells with smoothing=0.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'bin': ['A'] * 4 + ['B'] * 4, 'y': [0, 0, 0, 1, 0, 1, 1, 1]})\nresult = m.information_value(data, 'bin', 'y', smoothing=0)\nassert np.isclose(result[1], np.log(3))\nassert np.allclose(result[0][[\"good_share\", \"bad_share\"]].sum(), 1)",
    "check": "assert np.isclose(result[1], np.log(3))\nassert np.allclose(result[0][[\"good_share\", \"bad_share\"]].sum(), 1)",
    "interpretation": "All cells are positive, allowing smoothing=0. The unsmoothed IV is log(3).",
    "reference": "https://doi.org/10.1214/aoms/1177729694",
    "test": "tests/test_methods.py::test_information_value",
    "parameters": {
      "df": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "feature": [
        "str",
        "Pre-binned feature column name; no automatic binning is performed."
      ],
      "target": [
        "str",
        "Binary target column name, with both 0 and 1 present."
      ],
      "smoothing": [
        "float, default 0.5",
        "Finite nonnegative pseudo-count added to every contingency cell. Zero requires positive raw cells."
      ]
    },
    "summary": "Measure separation of pre-binned feature distributions between binary outcomes.",
    "title": "Information value"
  },
  {
    "name": "lgd_t_test",
    "family": "LGD",
    "returns": "pandas.DataFrame\n    Portfolio/segment rows: segment, N, realised_lgd_mean, pred_lgd_mean, s2 (sample error variance), mean_error (realised minus predicted), t_stat, p_value.",
    "notes": "Paired errors e=realised-expected LGD; t=mean(e)/sqrt(sample_variance(e)/N); p_value=t.sf(t,N-1). The one-sided alternative is underestimation (positive mean error). Independent normal errors give exact finite-sample t inference. Each segment needs at least two observations and nonzero error variance. Segment p-values are unadjusted. Each account has equal weight.",
    "raises": "Invalid level/segment, columns/LGDs, fewer than two observations per group or zero error variance.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'ead': [100, 200, 100, 100], 'predicted': [.2, .4, .6, .8], 'realised': [.1, .5, .4, .9], 'segment': ['A', 'A', 'B', 'B']})\nresult = m.lgd_t_test(data, 'realised', 'predicted', level='segment', segment_col='segment')\nassert result.segment.tolist() == ['A', 'B']\nassert np.isclose(result.loc[0, 'p_value'], .5)\nassert result.loc[1, 't_stat'] < 0",
    "check": "assert result.segment.tolist() == ['A', 'B']\nassert np.isclose(result.loc[0, 'p_value'], .5)\nassert result.loc[1, 't_stat'] < 0",
    "interpretation": "Segment A has zero average error and p=0.5. Segment B has negative mean error, opposite the underestimation alternative.",
    "reference": "https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.ttest_rel.html",
    "test": "tests/test_methods.py::test_lgd_t_test",
    "parameters": {
      "df": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "observed_lgd": [
        "str",
        "Realised LGD column name, finite fractions in [0, 1]."
      ],
      "expected_lgd": [
        "str",
        "Predicted LGD column name, finite fractions in [0, 1]."
      ],
      "level": [
        "{'portfolio', 'segment', 'pool'}, default 'portfolio'",
        "Test the whole portfolio or each segment; 'pool' aliases 'segment'."
      ],
      "segment_col": [
        "str, optional",
        "Required segment column for segment/pool tests; ignored at portfolio level."
      ]
    },
    "summary": "Test whether mean realised LGD exceeds mean expected LGD using paired errors.",
    "title": "LGD t-test"
  },
  {
    "name": "migration_matrix_stability",
    "family": "Stability",
    "returns": "tuple of pandas.DataFrame\n    (z_table, normal_cdf_table), square with the same ordered grades. Diagonals, empty rows and zero-variance comparisons are NaN.",
    "notes": "For off-diagonal probability f and adjacent probability n one step nearer the diagonal, z=(n-f)/sqrt((f*(1-f)+n*(1-n)+2*f*n)/N_i). Return Phi(z), as in ECB 2019 instructions. Small CDFs indicate violations of decreasing off-diagonal mass. These are asymptotic multinomial comparisons, not time-series equality tests. Retain empty grades. NaN means undefined, not passed. Cell probabilities are not multiplicity-adjusted.",
    "raises": "Invalid grade columns or rating order.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'start': [1] * 4 + [2] * 4 + [3] * 4, 'end': [1, 1, 2, 3, 1, 2, 2, 3, 1, 2, 3, 3]})\nresult = m.migration_matrix_stability(data, 'start', 'end')\nassert np.isclose(result[0].loc[1, 2], 2 / np.sqrt(11))\nassert np.isnan(np.diag(result[0])).all()",
    "check": "assert np.isclose(result[0].loc[1, 2], 2 / np.sqrt(11))\nassert np.isnan(np.diag(result[0])).all()",
    "interpretation": "Cell (1,2) compares 2/4 on the diagonal against 1/4 nearby. Cell (1,3) has equal adjacent probabilities, giving CDF 0.5.",
    "reference": "https://www.bankingsupervision.europa.eu/activities/internal_models/shared/pdf/instructions_validation_reporting_credit_risk.en.pdf",
    "test": "tests/test_methods.py::test_migration_matrix_stability",
    "parameters": {
      "df": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "initial_ratings_col": [
        "str",
        "Initial grade column name, paired by DataFrame row with final grades."
      ],
      "final_ratings_col": [
        "str",
        "Final grade column name, on the same scale as initial grades."
      ],
      "rating_order": [
        "sequence, optional",
        "Unique complete grade labels from lowest to highest; unobserved grades are retained. Otherwise use consistent ordered categorical metadata, then naturally sort the observed union. Specify business order explicitly."
      ]
    },
    "summary": "Calculate ECB adjacent-cell migration z statistics and their normal CDFs.",
    "title": "Migration matrix stability"
  },
  {
    "name": "population_stability_index",
    "family": "Stability",
    "returns": "tuple\n    (table, PSI). Bin-indexed table columns: expected, actual (normalized smoothed shares), PSI contributions; scalar sums contributions.",
    "notes": "Require exactly two samples and shared pre-defined bins. Add smoothing per sample/bin cell, normalize samples to shares E,A, then PSI=sum((A-E)*log(A/E)). Defaults choose expected/actual in natural or categorical order; explicit labels are clearer. Default smoothing=0.5; zero requires positive cells. PSI is symmetric and descriptive, not a significance test. Binning and smoothing change the value; no universal cutoffs are imposed.",
    "raises": "Missing columns, other than two samples, invalid/partial sample labels, invalid smoothing or zero unsmoothed cells.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'period': ['old'] * 4 + ['new'] * 4, 'bin': ['A', 'A', 'A', 'B', 'A', 'B', 'B', 'B']})\nresult = m.population_stability_index(data, 'period', 'bin', expected='old', actual='new', smoothing=0)\nassert np.isclose(result[1], np.log(3))\nassert np.allclose(result[0][[\"expected\", \"actual\"]].sum(), 1)",
    "check": "assert np.isclose(result[1], np.log(3))\nassert np.allclose(result[0][[\"expected\", \"actual\"]].sum(), 1)",
    "interpretation": "Shares change from (.75,.25) to (.25,.75), giving PSI=log(3). Significance requires a separate sampling model.",
    "reference": "https://doi.org/10.1214/aoms/1177729694",
    "test": "tests/test_methods.py::test_population_stability_index",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "bin_flag": [
        "str",
        "Sample/period column name, containing exactly two distinct labels."
      ],
      "variable": [
        "str",
        "Shared pre-defined bin column name; uses the union of observed bins."
      ],
      "expected": [
        "scalar, optional",
        "Reference sample label, specified together with actual. Defaults to the first sorted sample label."
      ],
      "actual": [
        "scalar, optional",
        "Comparison sample label, distinct from expected. Defaults to the second sorted label."
      ],
      "smoothing": [
        "float, default 0.5",
        "Finite nonnegative pseudo-count added to every contingency cell. Zero requires positive raw cells."
      ]
    },
    "summary": "Compare two distributions on a common set of pre-defined bins using PSI.",
    "title": "Population stability index"
  },
  {
    "name": "kendall_tau",
    "family": "Association",
    "returns": "tuple of float\n    (tau, p_value), testing zero association two-sided.",
    "notes": "Kendall tau compares concordant and discordant pairs. Variant b adjusts ties in both variables; c normalizes using distinct categories. Both equal tau-a without ties. The coefficient lies in [-1,1]. Independent pairs are needed for inference. SciPy selects exact/asymptotic inference as applicable; missing values are never silently omitted.",
    "raises": "Invalid/nonconstant paired vectors or unsupported variant.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\nresult = m.kendall_tau([1, 2, 3, 4], [1, 2, 4, 8])\nassert np.isclose(result[0], 1)\nassert np.isclose(result[1], 1 / 12)",
    "check": "assert np.isclose(result[0], 1)\nassert np.isclose(result[1], 1 / 12)",
    "interpretation": "All pairs concord: tau=1. The exact two-sided p-value for four distinct observations is 2/4!=1/12.",
    "reference": "https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.kendalltau.html",
    "test": "tests/test_methods.py::test_kendall_tau",
    "parameters": {
      "x": [
        "array-like",
        "Finite nonconstant one-dimensional numeric vector, with at least two observations."
      ],
      "y": [
        "array-like",
        "Equal-length finite nonconstant numeric vector, paired by position with x. Series indices are ignored."
      ],
      "variant": [
        "{'b', 'c'}, default 'b'",
        "Kendall tie normalization."
      ]
    },
    "summary": "Calculate Kendall ordinal association with the requested tie normalization.",
    "title": "Kendall's tau"
  },
  {
    "name": "somersd",
    "family": "Association",
    "returns": "object\n    SciPy SomersDResult with statistic, pvalue and table. Statistic is D(second | first).",
    "notes": "Somers D divides concordance minus discordance by pairs untied in the first (row/independent) variable. Swapping inputs can change D. Accept ranking vectors or a contingency table with ordered rows/columns. The null is D=0, with two-sided/less/greater alternatives. SciPy uses asymptotic normal inference for independent pairs; sparse tables weaken the approximation.",
    "raises": "Invalid vectors/alternative/table; tables need nonnegative integer counts and at least two nonempty rows and columns.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\nresult = m.somersd([[3, 1], [1, 3]])\nassert np.isclose(result.statistic, .5)",
    "check": "assert np.isclose(result.statistic, .5)",
    "interpretation": "Row-conditional association is 0.5. Tests also use asymmetric tables to check directionality.",
    "reference": "https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.somersd.html",
    "test": "tests/test_methods.py::test_somersd",
    "parameters": {
      "array_1": [
        "array-like",
        "Numeric ranking vector or nonnegative integer contingency table with ordered rows and columns."
      ],
      "array_2": [
        "array-like, optional",
        "Equal-length ranking vector; omit when array_1 is a table."
      ],
      "alternative": [
        "{'two-sided', 'less', 'greater'}, default 'two-sided'",
        "Alternative against zero association: nonzero, negative, or positive."
      ]
    },
    "summary": "Calculate asymmetric Somers D of the second variable conditional on the first.",
    "title": "Somers' D"
  },
  {
    "name": "spearman_correlation",
    "family": "Association",
    "returns": "object\n    SciPy SignificanceResult with statistic and pvalue.",
    "notes": "Spearman correlation is Pearson correlation of average ranks. It measures monotone association and handles ties using average ranks. SciPy returns an asymptotic p-value for zero rank correlation under the chosen alternative. Small-sample p-values are unreliable; consider permutation inference. Require at least three independent pairs and no constant arrays.",
    "raises": "Invalid alternative/vectors, fewer than three pairs, or constant inputs.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\nresult = m.spearman_correlation([1, 2, 3, 4], [1, 2, 4, 8])\nassert np.isclose(result.statistic, 1)",
    "check": "assert np.isclose(result.statistic, 1)",
    "interpretation": "The relationship is strictly increasing: rank correlation equals 1 despite a nonlinear scale.",
    "reference": "https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.spearmanr.html",
    "test": "tests/test_methods.py::test_spearman_correlation",
    "parameters": {
      "array_1": [
        "array-like",
        "Finite nonconstant one-dimensional numeric vector, paired by position with array_2."
      ],
      "array_2": [
        "array-like",
        "Equal-length finite nonconstant numeric vector; Series indices are ignored."
      ],
      "alternative": [
        "{'two-sided', 'less', 'greater'}, default 'two-sided'",
        "Alternative against zero association: nonzero, negative, or positive."
      ]
    },
    "summary": "Calculate Spearman rank correlation and an asymptotic association p-value.",
    "title": "Spearman correlation"
  },
  {
    "name": "pearson_correlation",
    "family": "Association",
    "returns": "object\n    SciPy PearsonRResult with statistic, pvalue and standard result methods.",
    "notes": "Pearson r is the centered cross-product divided by the product of centered Euclidean norms, in [-1,1]. Calculation does not require normality; SciPy default p-values assume independent bivariate-normal pairs under zero correlation. Near-constant inputs can emit SciPy NearConstantInputWarning; inspect and rescale data. Alternatives are two-sided, less or greater.",
    "raises": "Invalid alternative/vectors, fewer than two pairs, or constant inputs.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\nresult = m.pearson_correlation([1, 2, 3, 4], [1, 2, 4, 8])\nassert np.isclose(result.statistic, 11.5 / np.sqrt(143.75))\nassert result.statistic < 1",
    "check": "assert np.isclose(result.statistic, 11.5 / np.sqrt(143.75))\nassert result.statistic < 1",
    "interpretation": "Pearson r is about 0.959, below Spearman r=1: monotone does not imply exactly linear.",
    "reference": "https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.pearsonr.html",
    "test": "tests/test_methods.py::test_pearson_correlation",
    "parameters": {
      "array_1": [
        "array-like",
        "Finite nonconstant one-dimensional numeric vector, paired by position with array_2."
      ],
      "array_2": [
        "array-like",
        "Equal-length finite nonconstant numeric vector; Series indices are ignored."
      ],
      "alternative": [
        "{'two-sided', 'less', 'greater'}, default 'two-sided'",
        "Alternative against zero association: nonzero, negative, or positive."
      ]
    },
    "summary": "Calculate Pearson product-moment correlation and its association p-value.",
    "title": "Pearson correlation"
  },
  {
    "name": "migration_matrices_statistics",
    "family": "Stability",
    "returns": "tuple of float\n    (upper_MWB, lower_MWB), each in [0, 1]. Directions without migrations return 0.",
    "notes": "For each side of the diagonal, divide sum(|i-j|*N_ij) by sum(max(i,K-1-i)*N_ij), using zero-based grades. This is ECB normalized migration-weighted bandwidth. Upper means a later grade in rating_order; economic direction depends on that order. A side with no migrations has bandwidth 0 by convention. Describes distance, not frequency or significance.",
    "raises": "Invalid grade columns or rating order.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'start': [1] * 4 + [2] * 4 + [3] * 4, 'end': [1, 1, 2, 3, 1, 2, 2, 3, 1, 2, 3, 3]})\nresult = m.migration_matrices_statistics(data, 'start', 'end')\nassert np.allclose(result, [.8, .8])",
    "check": "assert np.allclose(result, [.8, .8])",
    "interpretation": "Each side has distance-weighted count 4 and maximum-distance-weighted count 5, giving 0.8.",
    "reference": "https://www.bankingsupervision.europa.eu/activities/internal_models/shared/pdf/instructions_validation_reporting_credit_risk.en.pdf",
    "test": "tests/test_methods.py::test_migration_matrices_statistics",
    "parameters": {
      "df": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "period_1_ratings": [
        "str",
        "Initial grade column name."
      ],
      "period_2_ratings": [
        "str",
        "Final grade column name, paired by row with initial grades."
      ],
      "rating_order": [
        "sequence, optional",
        "Unique complete grade labels from lowest to highest; unobserved grades are retained. Otherwise use consistent ordered categorical metadata, then naturally sort the observed union. Specify business order explicitly."
      ]
    },
    "summary": "Calculate ECB normalized migration-weighted bandwidth above and below the diagonal.",
    "title": "Migration-weighted bandwidth"
  },
  {
    "name": "conditional_information_entropy_ratio",
    "family": "Stability",
    "returns": "float\n    (H0-H1)/H0 in [0, 1].",
    "notes": "For rates p_i and normalized counts w_i, H0=h(sum(w_i*p_i)), H1=sum(w_i*h(p_i)), h(p)=-p*log(p)-(1-p)*log(1-p). Use natural logs and 0*log(0)=0. This is the fraction of binary uncertainty explained by grades. Zero counts contribute nothing; fractional weights are permitted. Descriptive, not a calibration p-value. A deterministic portfolio outcome makes the denominator zero.",
    "raises": "Invalid rates/counts/columns, nonpositive total count, or marginal rate 0 or 1.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'rate': [0, 1], 'n': [10, 10]})\nresult = m.conditional_information_entropy_ratio(data, 'rate', 'n')\nassert np.isclose(result, 1)",
    "check": "assert np.isclose(result, 1)",
    "interpretation": "Deterministic grade outcomes explain all marginal uncertainty in this balanced portfolio.",
    "reference": "https://doi.org/10.1002/j.1538-7305.1948.tb01338.x",
    "test": "tests/test_methods.py::test_conditional_information_entropy_ratio",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "realised_pd": [
        "str",
        "Grade-level realised default-rate column name, finite values in [0, 1]."
      ],
      "count": [
        "str",
        "Nonnegative finite count/weight column with positive total. Zero and fractional weights are allowed."
      ]
    },
    "summary": "Measure the fraction of marginal default uncertainty explained by grades.",
    "title": "Conditional information entropy ratio"
  },
  {
    "name": "kullback_leibler_dist",
    "family": "Stability",
    "returns": "float\n    H0-H1 in nats, between 0 and log(2): mutual information.",
    "notes": "The historical name denotes grade/default mutual information, not a general two-distribution KL function. Compute marginal minus count-weighted conditional binary entropy, with natural logs and 0*log(0)=0. Equivalently, average grade Bernoulli KL divergences from the portfolio Bernoulli distribution. Zero weights contribute nothing; all-zero/all-one outcomes return 0. No p-value applies.",
    "raises": "Invalid columns/rates/counts or nonpositive total count.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'rate': [0, 1], 'n': [10, 10]})\nresult = m.kullback_leibler_dist(data, 'rate', 'n')\nassert np.isclose(result, np.log(2))",
    "check": "assert np.isclose(result, np.log(2))",
    "interpretation": "Perfect grade separation removes log(2) nats of uncertainty from a balanced binary outcome.",
    "reference": "https://doi.org/10.1214/aoms/1177729694",
    "test": "tests/test_methods.py::test_kullback_leibler_dist",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "realised_pd": [
        "str",
        "Grade-level realised default-rate column name, finite values in [0, 1]."
      ],
      "count": [
        "str",
        "Nonnegative finite count/weight column with positive total. Zero and fractional weights are allowed."
      ]
    },
    "summary": "Calculate mutual information between rating grade and binary default outcome.",
    "title": "Kullback–Leibler information measure"
  },
  {
    "name": "loss_shortfall",
    "family": "LGD",
    "returns": "float\n    1 - total predicted loss / total realised loss; at most 1, with no finite lower bound.",
    "notes": "Monetary loss is EAD*LGD. Positive means aggregate underestimation, zero equal totals, negative overestimation. Nonnegative exposures need positive total; realised total loss must be positive. This descriptive aggregate can hide offsetting errors and is not a significance test.",
    "raises": "Invalid columns/LGDs/exposures or nonpositive total EAD/realised loss.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'ead': [100, 200, 100, 100], 'predicted': [.2, .4, .6, .8], 'realised': [.1, .5, .4, .9], 'segment': ['A', 'A', 'B', 'B']})\nresult = m.loss_shortfall(data, 'ead', 'predicted', 'realised')\nassert np.isclose(result, 0)",
    "check": "assert np.isclose(result, 0)",
    "interpretation": "Predicted and realised losses both total 240. MAD reveals individual errors hidden by this equality.",
    "reference": "https://www.bis.org/publ/bcbs_wp14.pdf",
    "test": "tests/test_methods.py::test_loss_shortfall",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "ead": [
        "str",
        "Finite nonnegative exposure column name, with positive total."
      ],
      "predicted_lgd": [
        "str",
        "Predicted LGD column name, finite fractions in [0, 1]."
      ],
      "realised_lgd": [
        "str",
        "Realised LGD column name, finite fractions in [0, 1]."
      ]
    },
    "summary": "Calculate relative underestimation of total exposure-weighted monetary loss.",
    "title": "Loss shortfall"
  },
  {
    "name": "mean_absolute_deviation",
    "family": "LGD",
    "returns": "float\n    Exposure-weighted mean absolute LGD error in [0, 1].",
    "notes": "MAD=sum(EAD*abs(realised-predicted LGD))/sum(EAD). Zero exposures do not contribute. Both LGDs use [0,1] fractions. This descriptive score measures error magnitude without cancellation; no hypothesis or p-value applies.",
    "raises": "Invalid columns/LGDs/exposures, negative EAD or nonpositive total EAD.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'ead': [100, 200, 100, 100], 'predicted': [.2, .4, .6, .8], 'realised': [.1, .5, .4, .9], 'segment': ['A', 'A', 'B', 'B']})\nresult = m.mean_absolute_deviation(data, 'ead', 'predicted', 'realised')\nassert np.isclose(result, .12)",
    "check": "assert np.isclose(result, .12)",
    "interpretation": "Absolute monetary error is 60 on EAD 500: MAD=0.12, or 12 LGD percentage points.",
    "reference": "https://www.bis.org/publ/bcbs_wp14.pdf",
    "test": "tests/test_methods.py::test_mean_absolute_deviation",
    "parameters": {
      "data": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "ead": [
        "str",
        "Finite nonnegative exposure column name, with positive total."
      ],
      "predicted_lgd": [
        "str",
        "Predicted LGD column name, finite fractions in [0, 1]."
      ],
      "realised_lgd": [
        "str",
        "Realised LGD column name, finite fractions in [0, 1]."
      ]
    },
    "summary": "Calculate the exposure-weighted mean absolute LGD prediction error.",
    "title": "Mean absolute deviation"
  },
  {
    "name": "elbe_t_test",
    "family": "LGD",
    "returns": "pandas.DataFrame\n    One row: facilities, lgd_mean (realised), elbe_mean (predicted), t_stat, p_value.",
    "notes": "For paired realised LGD minus ELBE errors, t=mean(error)/sqrt(sample_variance(error)/N). Return a two-sided t p-value with N-1 degrees of freedom. Null: zero mean paired error. Independent normal errors support finite-sample inference; each facility has equal weight. Small p-values can indicate either underestimation or overestimation.",
    "raises": "Invalid columns/LGDs, fewer than two pairs, or zero error variance.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'ead': [100, 200, 100, 100], 'predicted': [.2, .4, .6, .8], 'realised': [.1, .5, .4, .9], 'segment': ['A', 'A', 'B', 'B']})\nresult = m.elbe_t_test(data, 'realised', 'predicted')\nassert np.isclose(result.lgd_mean.iloc[0], .475)\nassert result.t_stat.iloc[0] < 0\nassert 0 < result.p_value.iloc[0] < 1",
    "check": "assert np.isclose(result.lgd_mean.iloc[0], .475)\nassert result.t_stat.iloc[0] < 0\nassert 0 < result.p_value.iloc[0] < 1",
    "interpretation": "Realised mean LGD is 0.475 versus ELBE 0.5. This equal-facility test differs from an exposure-weighted score.",
    "reference": "https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.ttest_rel.html",
    "test": "tests/test_methods.py::test_elbe_t_test",
    "parameters": {
      "df": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "lgd": [
        "str",
        "Realised LGD column name, finite fractions in [0, 1]."
      ],
      "elbe": [
        "str",
        "Expected loss best estimate column name, finite LGD fractions in [0, 1]."
      ]
    },
    "summary": "Test equality of mean realised LGD and ELBE using a two-sided paired t test.",
    "title": "Expected loss best estimate (ELBE) t-test"
  },
  {
    "name": "normal_test",
    "family": "Calibration",
    "returns": "pandas.DataFrame\n    One row: estimate (mean realised-minus-predicted PD), t_stat (historical name for normal z), p_value, outcome (boolean rejection).",
    "notes": "For T annual observations of one grade, e=realised-predicted PD; s2=sum((e-mean(e))**2)/(T-1). Basel z=sum(e)/sqrt(T*s2); p_value=normal.sf(z). Alternative: PD underestimation; reject when p_value < alpha. Assumes independent annual errors with common finite variance and sufficient years. Uses a normal, not Student t, reference; preserves historical column t_stat. Not an obligor-level Bernoulli test.",
    "raises": "Invalid alpha/rates/pairing, fewer than two years or zero annual-error variance.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\nresult = m.normal_test([.1, .1, .1, .1], [.1, .2, .3, .4])\nassert np.isclose(result.t_stat.iloc[0], .6 / np.sqrt(1 / 15))\nassert bool(result.outcome.iloc[0])",
    "check": "assert np.isclose(result.t_stat.iloc[0], .6 / np.sqrt(1 / 15))\nassert bool(result.outcome.iloc[0])",
    "interpretation": "Errors 0, 0.1, 0.2, 0.3 give z about 2.324 and p about 0.010. Four years illustrate arithmetic, not a strong asymptotic basis.",
    "reference": "https://www.bis.org/publ/bcbs_wp14.pdf",
    "test": "tests/test_methods.py::test_normal_test",
    "parameters": {
      "predicted_pd": [
        "array-like",
        "At least two finite annual predicted rates in [0, 1] for one grade, in time order."
      ],
      "realised_pd": [
        "array-like",
        "Equal-length realised annual rates in [0, 1], paired by position. Series indices are ignored."
      ],
      "alpha": [
        "float, default 0.05",
        "One-sided significance level strictly between zero and one."
      ]
    },
    "summary": "Apply the Basel one-sided normal approximation to annual PD forecast errors.",
    "title": "Normal test"
  },
  {
    "name": "redelmeier_test",
    "family": "Calibration",
    "returns": "tuple of float\n    (z_statistic, two_sided_p_value). Identical prediction vectors return (0, 1).",
    "notes": "This Redelmeier-style comparison explicitly assumes independent Y_i~Bernoulli(q_i), q_i=(p1_i+p2_i)/2, with fixed forecasts. Paired squared-loss difference is (p1-p2)*(p1+p2-2Y), with null mean zero and variance (p1-p2)**2*(p1+p2)*(2-p1-p2). Sum differences and variances to form z; return 2*normal.sf(abs(z)). Positive z favors the second forecast. This normal approximation uses the stronger midpoint null, not unrestricted equality of average Brier scores. The variance is derived from this stated model; the original paper motivates paired Brier comparisons, not certification of this convention. Identical forecasts provide no comparative evidence.",
    "raises": "Missing columns, nonbinary outcomes or invalid probabilities.",
    "example": "import numpy as np\nimport pandas as pd\n\nimport meliora as m\n\ndata = pd.DataFrame({'y': [0, 1, 1, 0], 'p1': [.1, .4, .7, .3], 'p2': [.2, .6, .6, .1]})\nresult = m.redelmeier_test(data, default_flag='y', first_pd='p1', second_pd='p2')\nassert np.isclose(result[0], .18 / np.sqrt(.0798))\nassert 0 < result[1] < 1",
    "check": "assert np.isclose(result[0], .18 / np.sqrt(.0798))\nassert 0 < result[1] < 1",
    "interpretation": "First-forecast total squared loss exceeds the second by 0.18. Swapping forecasts reverses z and preserves the two-sided p-value.",
    "reference": "https://pubmed.ncbi.nlm.nih.gov/1941009/",
    "test": "tests/test_methods.py::test_redelmeier_test",
    "parameters": {
      "df": [
        "pandas.DataFrame",
        "Nonempty table with unique columns and no missing required values. Extra columns are ignored; input is not modified."
      ],
      "default_flag": [
        "str, default 'DEFAULT_FLAG'",
        "Binary 0/1 outcome column name."
      ],
      "first_pd": [
        "str, default 'ADJUSTED_PD'",
        "First fixed forecast probability column name, values in [0, 1]."
      ],
      "second_pd": [
        "str, default 'min_PD'",
        "Second fixed forecast probability column name, values in [0, 1]."
      ]
    },
    "summary": "Compare paired Brier losses under an explicit midpoint-Bernoulli null model.",
    "title": "Redelmeier-style test"
  }
]
